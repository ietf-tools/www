name: Run accessibility tests
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
  push:
    branches:
      - '**'

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Download latest DB / media dump
        env:
          DEBIAN_FRONTEND: noninteractive
          AWS_ACCESS_KEY_ID: ${{ secrets.DO_SPACES_DEV_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.DO_SPACES_DEV_ACCESS_KEY }}
          AWS_DEFAULT_REGION: us-east-1
        run: |
          aws s3 cp --quiet --endpoint-url=${{ secrets.DO_SPACES_DEV_ENDPOINT }} s3://wagtail/ietfa.torchbox.latest.gz ./docker/database/ietfa.torchbox.latest.gz
          aws s3 cp --quiet --endpoint-url=${{ secrets.DO_SPACES_DEV_ENDPOINT }} s3://wagtail/media.tgz media.tgz

      - name: Unarchive media files
        run: |
          mkdir -p ./media
          tar -xzf media.tgz -C media

      - name: Docker Compose up
        run: docker compose up --build -d

      - name: Run accessibility tests
        run: |
          docker ps
          docker exec www-frontend-1 sh -c 'yarn test $APP_SERVER_URL'
